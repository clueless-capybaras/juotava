export default class RecipeList {
    constructor(
        title, createdBy, recipes
    ) {
        this.title = title;
        this.recipes = recipes;
        this.createdBy = createdBy;
    }
}