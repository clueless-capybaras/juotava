export default class RecipeExcerptsList {
    constructor(
        title, createdBy, excerpts
    ) {
        this.title = title;
        this.excerpts = excerpts;
        this.createdBy = createdBy;
    }
}